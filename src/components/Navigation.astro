---
import LanguagePicker from './LanguagePicker.astro';
import { getLangFromUrl, useTranslatedPath } from '../i18n/utils';
import { ui } from '../i18n/ui';

const lang = getLangFromUrl(Astro.url);
const t = ui[lang];
const translatePath = useTranslatedPath(lang);

const navLinks = [
  { href: '/', label: 'Home' },
  { href: '/projects', label: 'Projects' },
  { href: '/resume', label: 'Resume' },
  { href: '/contact', label: 'Contact' },
];
---

<nav class="main-nav">
  <div class="nav-container">
    <a href={translatePath('/')} class="logo">RM</a>
    
    <div class="nav-links">
      {navLinks.map(link => (
        <a href={translatePath(link.href)} class="nav-link">{link.label}</a>
      ))}
    </div>

    <div class="nav-controls">
      <button id="theme-toggle" class="theme-toggle" aria-label="Toggle theme">
        <svg class="sun-icon" xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <circle cx="12" cy="12" r="5"></circle>
          <line x1="12" y1="1" x2="12" y2="3"></line>
          <line x1="12" y1="21" x2="12" y2="23"></line>
          <line x1="4.22" y1="4.22" x2="5.64" y2="5.64"></line>
          <line x1="18.36" y1="18.36" x2="19.78" y2="19.78"></line>
          <line x1="1" y1="12" x2="3" y2="12"></line>
          <line x1="21" y1="12" x2="23" y2="12"></line>
          <line x1="4.22" y1="19.78" x2="5.64" y2="18.36"></line>
          <line x1="18.36" y1="5.64" x2="19.78" y2="4.22"></line>
        </svg>
        <svg class="moon-icon" xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"></path>
        </svg>
      </button>
      <LanguagePicker />
    </div>
  </div>
</nav>

<style>
  .main-nav {
    padding: 1.25rem 0;
    border-bottom: 1px solid var(--border-color);
    background-color: var(--bg-color);
    color: var(--text-color);
    position: sticky;
    top: 0;
    z-index: 100;
    transition: background-color var(--transition-duration, 0.4s) var(--transition-ease, cubic-bezier(0.4, 0, 0.2, 1)),
                border-color var(--transition-duration, 0.4s) var(--transition-ease, cubic-bezier(0.4, 0, 0.2, 1)),
                color var(--transition-duration, 0.4s) var(--transition-ease, cubic-bezier(0.4, 0, 0.2, 1)),
                backdrop-filter 0.3s ease;
  }

  .main-nav.scrolled {
    background-color: rgba(255, 255, 255, 0.85);
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
  }

  :global(.dark) .main-nav.scrolled {
    background-color: rgba(26, 26, 26, 0.85);
  }

  .nav-container {
    max-width: 1400px;
    margin: 0 auto;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 2rem;
    width: 100%;
    padding: 0 5vw;
  }

  .logo {
    font-size: 1.125rem;
    font-weight: 500;
    text-decoration: none;
    color: var(--text-color);
    letter-spacing: 0.05em;
  }

  .nav-links {
    display: flex;
    gap: 2rem;
    flex-wrap: wrap;
  }

  .nav-link {
    text-decoration: none;
    color: var(--text-color);
    font-size: 0.875rem;
    opacity: 0.7;
    transition: opacity 0.2s;
  }

  .nav-link:hover {
    opacity: 1;
  }

  .nav-controls {
    display: flex;
    align-items: center;
    gap: 1rem;
  }

  .theme-toggle {
    background: none;
    border: none;
    cursor: pointer;
    padding: 0.375rem;
    color: var(--text-color);
    display: flex;
    align-items: center;
    justify-content: center;
    opacity: 0.7;
    transition: opacity 0.2s, color var(--transition-duration, 0.4s) var(--transition-ease, cubic-bezier(0.4, 0, 0.2, 1));
    position: relative;
    width: 1.5rem;
    height: 1.5rem;
  }

  .theme-toggle svg {
    position: absolute;
    transition: transform var(--transition-duration, 0.4s) var(--transition-ease, cubic-bezier(0.4, 0, 0.2, 1)),
                opacity var(--transition-duration, 0.4s) var(--transition-ease, cubic-bezier(0.4, 0, 0.2, 1));
  }

  .theme-toggle:hover svg {
    transform: rotate(15deg);
  }

  .sun-icon {
    opacity: 0;
    transform: rotate(-90deg);
  }

  .moon-icon {
    opacity: 1;
    transform: rotate(0deg);
  }

  :global(.dark) .sun-icon {
    opacity: 1;
    transform: rotate(0deg);
  }

  :global(.dark) .moon-icon {
    opacity: 0;
    transform: rotate(90deg);
  }

  @media (max-width: 768px) {
    .nav-container {
      flex-wrap: wrap;
      justify-content: space-between;
      padding: 0 1.5rem;
    }

    .nav-links {
      order: 3;
      width: 100%;
      justify-content: center;
      gap: 1.25rem;
    }
  }
</style>
