---
import Layout from '../../../layouts/Layout.astro';

const lang = 'cs';
---

<Layout title="Životopis - Richard Matečka">
  <div class="resume-container">
    <header class="resume-header">
      <p class="resume-label">Životopis</p>
      <h1 class="resume-name">Richard Matečka</h1>
      <div class="resume-divider"></div>
      <p class="resume-tagline">
        Full-stack vývojář s vášní pro tvorbu elegantních řešení
        a objevování nových technologií.
      </p>
    </header>

    <section class="resume-section">
      <h2 class="section-title">Dovednosti</h2>
      
      <div class="skills-grid">
        <div class="skill-category">
          <h3 class="category-title">Technické</h3>
          <p class="skill-text">Webový vývoj s moderními frameworky a best practices.</p>
        </div>

        <div class="skill-category">
          <h3 class="category-title">Nástroje</h3>
          <p class="skill-text">Tvorba interaktivních zážitků s pozorností k detailu.</p>
        </div>

        <div class="skill-category">
          <h3 class="category-title">Měkké dovednosti</h3>
          <p class="skill-text">Řešení problémů a kreativní myšlení.</p>
        </div>

        <div class="skill-category">
          <h3 class="category-title">Jazyky</h3>
          <p class="skill-text">Angličtina (rodný), Čeština (rodný), Němčina (základy).</p>
        </div>
      </div>
    </section>

  </div>
</Layout>

<style>
  .resume-container {
    max-width: 800px;
    margin: 0 auto;
    padding: 4rem 2rem;
  }

  .resume-header {
    text-align: left;
    margin-bottom: 4rem;
    padding-bottom: 3rem;
    border-bottom: 1px solid var(--border-color);
  }

  .resume-label {
    font-size: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 0.3em;
    color: var(--text-color);
    opacity: 0.5;
    margin: 0 0 1rem 0;
  }

  .resume-name {
    font-size: clamp(2.5rem, 8vw, 4rem);
    font-weight: 300;
    margin: 0 0 1.5rem 0;
    color: var(--text-color);
    letter-spacing: -0.02em;
    line-height: 1.1;
  }

  .resume-divider {
    width: 40px;
    height: 1px;
    background: var(--text-color);
    opacity: 0.3;
    margin: 0 0 1.5rem 0;
  }

  .resume-tagline {
    font-size: 1rem;
    color: var(--text-color);
    opacity: 0.7;
    line-height: 1.7;
    margin: 0;
    max-width: 500px;
  }

  .resume-section {
    margin-bottom: 4rem;
  }

  .section-title {
    font-size: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 0.2em;
    font-weight: 600;
    margin: 0 0 2rem 0;
    color: var(--text-color);
    opacity: 0.6;
  }

  .experience-item,
  .education-item {
    margin-bottom: 2.5rem;
  }

  .experience-header,
  .education-header {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
    align-items: baseline;
    margin-bottom: 0.75rem;
  }

  .job-title,
  .degree {
    font-size: 1.125rem;
    font-weight: 500;
    margin: 0;
    color: var(--text-color);
  }

  .company,
  .school {
    font-weight: 400;
    color: var(--text-color);
    opacity: 0.7;
  }

  .period {
    font-size: 0.875rem;
    color: var(--text-color);
    opacity: 0.5;
  }

  .experience-description,
  .education-description {
    margin: 0;
    font-size: 0.9375rem;
    color: var(--text-color);
    opacity: 0.75;
    line-height: 1.7;
    max-width: 650px;
  }

  .skills-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
    gap: 2rem;
  }

  .skill-category {
    padding: 0;
  }

  .category-title {
    font-size: 0.875rem;
    font-weight: 500;
    margin: 0 0 0.75rem 0;
    color: var(--text-color);
  }

  .skill-text {
    font-size: 0.875rem;
    color: var(--text-color);
    opacity: 0.6;
    line-height: 1.6;
    margin: 0;
  }

  @media (max-width: 768px) {
    .resume-header {
      text-align: center;
    }

    .resume-divider {
      margin: 0 auto 1.5rem auto;
    }

    .experience-header,
    .education-header {
      flex-direction: column;
      gap: 0.25rem;
    }
  }
</style>
